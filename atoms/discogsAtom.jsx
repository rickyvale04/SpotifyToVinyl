import { atom } from "recoil";

export const discogsUserState = atom({
    key: "discogsUserState",
    default: null
});

export const discogsTokenState = atom({
    key: "discogsTokenState", 
    default: null
});
