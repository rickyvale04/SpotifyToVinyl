import { signOut } from "next-auth/react";

const UserInfo = ({ user }) => {
  return (
    <div className="container mx-auto p-4 flex flex-col items-center space-y-4">
      {/* User profile information has been removed as per request */}
    </div>
  );
};

export default UserInfo;
